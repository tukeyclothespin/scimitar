# If using batch size = 1, this can be false
# Otherwise a batch size > 1 requires images of the same size.
ONE_IMAGE_SIZE = True
# Most of the AcTiV labeled samples are 720x576. AlJazeeraHD is not
INPUT_WIDTH = 720
INPUT_HEIGHT = 576
